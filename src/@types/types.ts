export interface User {
    id: number;
    name: string;
    email: string;
    phone: string;
    address: string;
    roles: string[];
    addedBy: string;
    createdAt: string;
    updatedAt: string;
    enabled: boolean;
}
